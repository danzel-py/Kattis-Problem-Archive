#include <iostream>
using namespace std;

int main(){
    int tc;
    cin>>tc;
    for (int i = 0; i < tc; i++)
    {
        int phone;
        string arr[phone];
        for (int j = 0; j < phone; j++)
        {
            cin>>arr[j];    
        }
        
    }
    
    return 0;
}